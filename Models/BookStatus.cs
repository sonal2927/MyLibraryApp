namespace LibraryManagementSystem.Models
{
    public enum BookStatus
    {
        Pending,
        Approved,
        Issued,
        Returned,
        Rejected,
        ReturnRequested,
        RenewalRequested
    }
}
